---
type: content
animate: slide-left
---

# The Terminal: Not Scary, I Promise

Four commands. That's all you need to start.

```
$ ls              # list files in a folder
$ cd folder-name  # go into a folder
$ mkdir name      # create a new folder
$ claude          # start Claude Code
```

A good terminal to start with is Ghostty — ==ghostty.org==. It's free, looks clean, zero configuration effort. But any terminal works.

---notes---

"Before I show you the demos, let me quickly show you the tool I'll be using. This is a terminal. If you've never used one — it's just a text-based way to talk to your computer. Instead of clicking buttons, you type commands."

"Here are the only four things you need to know:"

"ls to see what's here, cd to go somewhere, mkdir to create a folder, claude to start working. Four commands. That's all you need to start."

"A good terminal to start with is Ghostty — ghostty.org. It's free, looks clean, zero configuration effort. But any terminal works."

- Keep it FAST — this is normalization, not a tutorial
- Show Ghostty because it looks modern and non-intimidating
- Only 4 commands — resist the urge to show more
- The point is: "this isn't scary, you can do this"
- Don't say "command line" — say "terminal" (less intimidating)
